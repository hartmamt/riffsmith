[package]
name = "string-engine"
version = "0.1.0"
edition = "2021"
description = "RiffSmith hybrid string model: sampled-attack-excited digital waveguide strings (wasm32)"
license = "MIT"

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = 3
lto = true
panic = "abort"
codegen-units = 1
